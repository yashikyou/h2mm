import { container, subtitle } from './footer.module.scss';

const Footer = () => (
  <div className={container}>
    <p className={subtitle}>happy to make money ❤</p>
  </div>
);

export default Footer;
